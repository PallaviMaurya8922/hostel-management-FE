export const config = {
  apiBaseUrl: "https://hostel-management-be-jg5f.onrender.com/api",
  apiTimeout: 10000,
  env: 'development',
  // isDevelopment: import.meta.env.DEV,
  // isProduction: import.meta.env.PROD,
} as const;
